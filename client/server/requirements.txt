fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
pydantic==2.10.4
python-multipart==0.0.20
pypdf==5.1.0
requests==2.32.3

# NLP. Without these the analyzer falls back to a regex tokenizer and the
# server still runs, just with coarser instructions.
spacy==3.8.2
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# Optional LLM enrichment pass. Needs OPENAI_API_KEY to actually run.
openai==1.59.6
